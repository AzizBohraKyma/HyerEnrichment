# 2026-07-13

## Disposable-email blocklist (mailchecker)

- Core dep `mailchecker` in `pyproject.toml`
- `EmailVerifier` rejects disposable domains before MX/AfterShip/Reacher (`status=disposable`)
- Runner confidence ignores disposable-only `verified_emails`
- Unit test: `user@mailinator.com`; frontend status union includes `disposable`

## Task 2 — LLM disambiguation with fallback providers

Implemented:
- `PipelineOrchestrator._disambiguate_handles()` in `runner.py`: walks handles below `DISAMBIGUATION_THRESHOLD`, `llm.compare(target, evidence)`, keep+boost or drop
- `_build_confidence` no longer does username-vs-unknown LLM call; records `disambiguation_dropped` in metadata
- Compose: litellm gets `env_file ../.env` + port 4000; api/worker get `LLM_*` passthrough
- `.env.example` documents OpenAI/Gemini vendor keys on litellm container only
- Unit tests: `tests/test_disambiguation.py` (4 passed)

## LiteLLM Docker fix + E2E (later same day)

- Empty `DATABASE_URL: ""` still crash-loops LiteLLM (`missing scheme`). Fix: do **not** env_file whole `.env`; pass only `OPENAI_API_KEY` / `GEMINI_API_KEY` via compose interpolation; add `docker/litellm_config.yaml` (wildcard model_list) because newer proxy rejects unlisted models.
- E2E: chat via proxy OK; `_disambiguate_handles` via `http://litellm:4000` kept X/jane_doe @0.95, dropped unrelated bot, kept GitHub/jane-doe; enrich/sync shows `llm disambiguation dropped: N` evidence.

## Free-sidecar smoke + GMaps Playwright CDN fix

- Root cause: Hub `gosom/google-maps-scraper:latest` + playwright-go v0.5700.1 downloads `playwright-1.57.0-linux.zip` from retired azureedge CDN → 404. Volume `gmaps_playwright_cache:/opt` also shadowed driver files.
- Fix: `Dockerfile.google-maps-scraper` builds upstream binary and assembles driver from npm `playwright-core@1.57.0` + nodejs.org into `/opt/node` + `/opt/package`; compose drops `/opt` volume.
- Smoke PASS: SA handles + GMaps business (`Hey Neighbor Cafe`). Strict E2E: gmaps create/poll/download PASS; gitrecon unset FAIL expected.

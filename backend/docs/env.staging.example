# Staging environment template (copy to backend/.env — never commit filled file)
# See docs/deployment.md

APP_ENV=staging
API_TOKEN=change-me
PROXY_MODE=none
# PROXY_MODE=scrapoxy
# SCRAPOXY_URL=http://scrapoxy:8888
ENABLE_TIER1=false

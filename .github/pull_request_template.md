## Summary

<!-- What changed and why (1–3 bullets). -->

## Checklist

- [ ] Searched for existing code to reuse; no duplicate validation/merge/hash/API mapping
- [ ] Tests or typecheck run for the area changed
- [ ] No secrets or raw PII in logs; opt-out / suppression not bypassed
- [ ] `backend/docs/ARCHITECTURE.md` **Implementation status** updated if scaffold vs target changed
- [ ] **ADR:** If this PR introduces or changes **storage**, **queue**, **auth**, or **layer ownership**, an ADR is linked below (new or existing under [`docs/adr/`](../docs/adr/)). Mark N/A only when none of those apply.

### ADR link (required when the box above applies)

<!-- Example: docs/adr/0002-sqlite-local-postgres-docker.md — or "N/A" -->

-
